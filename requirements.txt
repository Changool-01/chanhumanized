# Chan Humanized AI — runtime packages
Django>=5.1,<5.3
django-environ>=0.11,<0.13
openai>=1.40,<3
PyMySQL>=1.1,<2
gunicorn>=22,<26

# Rate-limiting and CSP are implemented in apps/accounts (no extra packages).
